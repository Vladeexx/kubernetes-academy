apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: brunospace
  name: pvc-mongoclaim
spec:
  storageClassName: "manual"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
