apiVersion: v1
kind: Service
metadata:
  namespace: task1
  name: demo-service
spec:
  selector:
    app: deploy1
  ports:
  - name: name-of-service-port
    protocol: TCP
    port: 80
    targetPort: 8081